// import { defineConfig } from 'astro/config';

// export default defineConfig({
//   site: 'https://zzz1750.github.io/TAG-BREAK/',
//   base: '/TAG-BREAK/', 
// });

import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://tagbreak.in',
  base: '/',
});
